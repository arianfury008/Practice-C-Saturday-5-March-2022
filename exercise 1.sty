1- coordinating all computer operations and performing arithmetic and logical operations on data.  عملیات کامپیوتری و انجام عملیات حسابی و منطقی روی داده ها

2- machine language - Assembly language - High-level language

3- Syntax errors are mistakes in using the language. مشکلاتی که در کد وجود دارد، بخاطر نادرست نوشتن کد ما نسب به ساختار اون زبان، مثلا سیمکالن  تو سی شارپ نزاری

4- Writing and editing the program - Compiling the program - Linking the program - Executing the program

5- A byte is the amount of storage required to store a single character, بایت ها مقدار فضایی هستن که می تونن یک کاراکتر را ذخیره کنند 'D'
binary digit or bit is the smallest a element that camputer can deal with . bit based on two number 0 and 1 باینری دیجیت یا بیت به کوچیک ترین مقداری که سیستم می تونه اجرا کنه و هر بین بر اساس دو عدد یک صفر تشکل می شود
there are 8 bits to a byte. هر بایت 8 بیت داره که یک سینگل کارکتر تشکیل میده

RAM : random access memory temporary storage of programs and data, مقدار فضا که همیشگی نیست و با خاموش شدن سیستم یا از دسترس خارج شدن برنامه مقدار خافظه پاک می شود
ROM : read-only memory which tores programs or data permanently.مقدار دیتا همیشگی ذخیره می کند برعکس رم

مقدار رید انلی مموری نمی تواند از تریغ کاربر عوض بشود بر عکس رم

Catch : The Cache Memory is the memory which is very nearest to the CPU that a computer microprocessor can access more quickly than it can access regular RAM.
کج مموری هم معلوم است از اسمش مقداری که نزیک به سیپو که ریزپردازنده های کامپیوتر سریع تر از مموری عادی دسترسی بهش دارد

همه لول های کچ مموری سریع تر از مموری است ولی لول 1 از همه سریع تر است و همین طور مقدار حافظه کمتری در ان دخیره می شود